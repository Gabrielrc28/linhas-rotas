package com.dimed.pocdimed.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.dimed.pocdimed.model.PontoDeTaxi;

public interface PontoDeTaxiRepository extends JpaRepository <PontoDeTaxi, String>{
}
